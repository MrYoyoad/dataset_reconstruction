# Independent audit of `theory/T1..T6` — yoado-1d, 2026-09-17

Auditor is the W1 / proofs lane, **not** the author of these files. Method per the approver's spec: re-derive each
statement from its assumptions without following the given proof, then compare; trace every assumption to where it
is *used*; attempt a counterexample; and confirm each numerical check tests the theorem rather than a consequence
that would hold anyway. Every entry carries its register. **read-prose is never a PASS.**

Checkable from: `de8b0ae` (theory files, harness), results `results/multilayer_cert/theory_checks_688036.jsonl`,
`theory_checks_683234.jsonl`, `survival_692603.jsonl`.

---

## F1 — T2 Proposition A: the mathematics is CONFIRMED (register: derived)

I re-derived the closure independently before reading the proof, and reached the same three invariants by the same
induction. Sketch of my derivation, for comparison:

- The `A`-update is `-eta s B_t^T D_t H_t^T`, whose **row space** lies in `col(H_t) subset Hcal`. Hence `A_t V' =
  A_0 V'` for any basis `V'` of `Hcal^perp`: the off-span block is never touched.
- `B_0 = 0`, so the `A`-update at `t=0` vanishes and `A_1 = A_0`. Inductively the `B`-update's **rows** lie in
  `col(A_t H_t) subset col(A_0 Hcal)`, and the `A`-update's **columns** lie in `sum_t row(B_t) subset
  col(A_0 Hcal)`. So `row(B_T) subset col(X')` and `col(A_T U') subset col(X')`, with `X' = A_0 U'`.
- Given `rank B_T = N'`, containment plus equal dimension forces `row(B_T) = col(X')`, so `P_{row(B_T)^perp}`
  annihilates `col(A_T U')`, and since every `H_{l,t}` has columns in `Hcal`, `C_full H_{l,t} = 0` for all `t`.
  `H_{l,0} = H_l^0` because `B_{l',0} = 0` at every upstream layer, so the base representation is the `t=0` member.

**This is the same theorem as W1's certificate theorem with `Hcal` replacing `col(H)` and `N'` replacing `q`.** The
substitution is exact, which is a strong consistency signal: two files derived independently reach the same closure.

## F2 — T2 Prop. A's stated RANK FORMULA IS WRONG, and contradicts Prop. A's own kernel formula

Prop. A states `rank C_full = r - N'`. That is false whenever `n_l < r`, and it disagrees with the *other*
conclusion of the same proposition. From `ker C_full = Hcal (+) ker A_0` and `rank A_0 = min(r, n_l)`:

    rank C_full = n_l - N' - max(0, n_l - min(r,n_l)) = max(0, min(r - N', n_l - N')).

| r | n_l | N' | stated `r-N'` | from Prop. A's own kernel | measured |
|---|---|---|---|---|---|
| 24 | 16 | 3 | **21** | 13 | **13** |
| 24 | 20 | 15 | **9** | 5 | **5** |
| 6 | 20 | 2 | 4 | 4 | 4 |
| 6 | 20 | 6 | 0 | 0 | 0 |

The stated form is wrong in **both exercised rows of the load-bearing check**. The proof's justification —
"because `A_0` is a.s. of full rank" — is the slip: `A_0` is `r x n_l`, so full rank means `min(r, n_l)`, not `r`.

This is not cosmetic. **The track's headline is that what depth costs is rank**, so the rank law is the headline
quantity, and the stated form overstates surviving rank by `r - n_l` on any layer narrower than the adapter rank.
The harness already uses `max(0, min(r-N', dims[l]-N'))` and the "certificate lifetime `T >= min(r,n_l)/N`" result
also uses `min(r,n_l)` — so **the code and one of the file's own results are right while the theorem statement is
wrong.** Fix the statement; nothing downstream needs to move.

**Status consequence:** T2 is marked PROVED. Prop. A's structural content is proved; its rank clause as written is
not, and should not travel in a Gal-facing document in its current form.

## F3 — the hypothesis list is TIGHTER than it looks (register: derived)

`row(B_T) subset col(X')` holds unconditionally and `dim col(X') <= N'`. So **B3 (`rank B_T = N'`) forces
`dim col(X') = N'`, i.e. `A_0` injective on `Hcal`.** The genericity condition one would expect to need separately
— and which W1 needed as an explicit hypothesis — is *implied* here. Worth stating: it makes B3 carry more than it
appears to, and it means no extra generic-position assumption is missing.

Correspondingly, B6's stated role ("`ker C_full = Hcal (+) ker A_0`") is only half right: the **directness** of the
sum comes from B3, and B6 is needed for the *dimension* of `ker A_0`, hence for the rank count in F2.

## F4 — the load-bearing numerical check is GENUINE, but passes by configuration rather than by construction

The track's rule is that a FAIL here falsifies the track, so the question is whether it can pass vacuously. It does
not, in this run (register: read-rows, `theory_checks_688036.jsonl`):

| layer | drift | N' | rank B_T | rank C | rho(H^0) | B3 | exercised |
|---|---|---|---|---|---|---|---|
| 0 | 0.00 | 3 | 3 | 13 | 4.2e-16 | yes | no (no drift) |
| 1 | 0.62 | 15 | 15 | **5** | 1.2e-15 | yes | **yes** |
| 2 | 2.13 | 15 | 15 | **5** | 8.8e-16 | yes | **yes** |
| 3 | 4.93 | 15 | 4 | 16 | **7.8e-2** | **NO** | no |

Both exercised rows have `rank C = 5 > 0`, so annihilation is a real constraint and not `0 = 0`; drift is 62% and
213%; and layer 3 is the predicted contamination mode, `rho` jumping eleven orders when B3 fails. The harness also
guards the failure modes its own LESSONS entry names: an explicit non-finite trap so a NaN cannot be swallowed by
`max()`, a drift *band* rather than "largest lr that stays finite", and an `exercised` filter that fails the check
as vacuous if no deep drifting layer satisfies B3.

**The gap:** `exercised` requires B3, `layer >= 1` and `drift > 1e-3` — but **not `rank C > 0`**. A layer with
`N' >= min(r, n_l)` has `C_full` numerically zero, and `rho = ||CH||/(||C|| ||H||)` is then a ratio of two
near-zero quantities that can land anywhere, including under tolerance. Here `N' = 15 < 20`, so it does not bite —
the test is sound *in this configuration*, by luck of the shape rather than by construction. Since Cor. A.1's whole
content is that `rank C` reaches 0, and the survival sweep reports 12 rows at `rank C = 0`, the guard should be
added before that sweep's rows are used to support Prop. A.

## F5 — T5.1 is CONFIRMED and it answers a question W1 records as OPEN (register: derived)

`Phi_l^0 = psi_l . Phi_{l-1}^0` gives `M_l = J_{psi_l} M_{l-1}`, so every block of `J_F` factors through `M_1`;
hence `ker M_1 subset ker J_F` and `rank J_F <= rank M_1 <= min(k, n_1)`. I derived this independently and it is
correct.

**Why it matters beyond this track:** W1 §6 lists the stacked-Jacobian rank across layers as *unmeasured*, and that
is precisely the third of the three counts — the only one that bears on identifiability. T5.1 supplies a proved
**upper bound** for it: no amount of depth exceeds the first adapted layer's chart sensitivity. That is a direct,
proved answer to the shape of Gal's "~200 equations" objection — the ceiling is `k_1`, not `sum_l (r_l - N)`.
Subject to this audit being accepted, W1's open clause can be upgraded from "unmeasured" to "bounded above by
`rank M_1` (T5.1); the value within that bound is unmeasured".

**How this must travel, because the obvious phrasing does damage.** "No amount of depth exceeds the first adapted
layer's chart sensitivity" reads as a blocker on the depth route. **It is not.** `k_1 = rank M_1 <= k`, so the
ceiling sits *at* the attacker's own chart dimension, not below it: T5.1 says depth cannot buy you more than the
chart you chose to search, which is a statement about the chart, not a limit on depth. Depth remains the mechanism
that *reaches* that ceiling when a single layer's budget `r - N` falls short of it — which is exactly T5.3's second
regime. Relay it as **"the ceiling on stacked information is the first adapted layer's chart sensitivity `k_1`, and
`k_1` is at most the chart dimension `k`"**, never as "depth adds nothing".

T5.4 is correctly marked CONJECTURE, and its own caveat — that the honest object under drift is *effective* rank at
a stated tolerance, not rank — is the right one and is the kind of distinction that usually has to be forced.

## F6 — the trajectory/endpoint scope line, and what the attacker can actually check

Gal's 15 Sept note observes that the base model and the released adapter give the features at *start and endpoint*
but not the intermediate trajectory. `Hcal = sum_{t<T} col(H_{l,t})` is exactly an object of the trajectory, so
`N'` is not attacker-computable. T2's assumption table says this ("`rank B_T` is observable; `N'` is not"), which
is honest and should stay.

The sharper form, and the one to state: the attacker **can** see when the channel is *empty* — `rank C_full` is
computed from the release alone, and `rank C_full = 0` is visible. What they **cannot** see is whether a *nonempty*
`C_full` is exact or contaminated, because that is `rank B_T = N'` and they do not have `N'`. So the failure modes
split: rank death is attacker-visible, contamination is not. This is the same shape as W1's corrected excitation
clause — **computable-from-the-release and verifiable-from-the-release are different properties** — and the
certificate-lifetime result (`T >= min(r,n_l)/N`) is consequently a **defender-facing** statement.

---

## Verdict

| item | verdict |
|---|---|
| T2 Prop. A, structural content | **CONFIRMED** independently (F1) |
| T2 Prop. A, stated rank formula | **FAIL** — wrong for `n_l < r`, contradicts its own kernel clause (F2) |
| T2 assumption trace | sound; B3 implies more than stated, B6's stated role half right (F3) |
| T2 load-bearing numerical check | genuine here, missing a `rank C > 0` guard (F4) |
| T5.1 | **CONFIRMED**, and upgrades an open clause in W1 (F5) |
| T5.4 | correctly CONJECTURE |
| attacker/experimenter scope | stated, and should be sharpened per F6 |

**T1, T3, T4, T6 are not yet audited.** T3 is a negative result (`O(eps)` sharp, with a closed-form
counterexample) and T4 is a counterexample catalogue, so both are lower-risk; T1 and T6 remain.

**Nothing here promotes any status on the strength of a numerical check agreeing.** F2 is a proof-level defect
found by derivation, and the measurements confirm the *corrected* formula rather than the stated one.

---

# ADDENDUM — the rows, across all four jobs and the survival sweep (register: read-rows)

Added after the GM's brief: audit the **rows**, not the write-up; report the **three outcomes** separately
(pass / fail / **vacuous**) rather than a pass rate; and check the selected rows are the B3-holding ones rather
than a subset chosen after seeing the residuals. Source: `results/multilayer_cert/survival_692603.jsonl`, 216 rows.

## F7 — the survival sweep's population, partitioned properly

| outcome | rows | meaning |
|---|---|---|
| flagged `diverged` | 16 | correctly excluded by the harness |
| **exploded but NOT flagged** | **9** | see F8 — the detector cannot see these |
| B3 fails (`rank B_T != N'`) | 75 | contamination regime; **not a test of Prop. A** |
| `rank C_full = 0` (VACUOUS) | 3 | `C_full` is the zero matrix; annihilation is `0 = 0` and tests nothing |
| **real tests of Prop. A** | **116** | of which **71** are deep *and* drifting |

Taken naively over every B3-holding row, `max rho_full = 7.10e-01` — which would read as a catastrophic FAIL of the
load-bearing check. It is not. Every row driving it is an **undetected divergence**.

## F8 — the divergence detector is blind at layer 0 by construction (the defect)

The worst row: `layer 0, delta = 0.0, delta_perp = 0.0, N' = 3, rank B_T = 3, B3 holds, diverged = False`, and
`rho_full = 0.71` with `rank C_full = 1` where the law predicts 13. It looks like a counterexample to Prop. A at
**zero drift on the first layer** — the easiest case in the study.

It is not. That row has **`beta = 1.28e+21`**: the adapter's operator norm exploded. `diverged` is computed from
**representation drift**, and at layer 0 the drift is identically zero however badly the adapter blows up, because
layer 0's inputs are the data. **So the detector can never fire at layer 0**, and an exploded run enters the
B3-holding population wearing `drift = 0`.

All **9** rank-law mismatches are of this family — `beta` from `1.28e+21` to `3.84e+302`, drift from `4.8e+43` to
`inf`. None is flagged. This is precisely the failure mode the track's own ground rules name ("an exploded net makes
every singular subspace noise while still looking like a large-drift test"), arriving through the one door the
guard does not cover.

## F9 — with a principled guard, every headline claim holds, and the rank law becomes exact

Excluding runs whose **adapter** exploded, rather than only those whose drift did:

| guard | rows | real | vacuous | max `rho_full` | above 1e-11 tol | rank-law mismatches |
|---|---|---|---|---|---|---|
| `beta <= 1e6` | 116 | 116 | **0** | **2.68e-13** | **0** | **0** |
| `beta <= 1e8` | 116 | 116 | 0 | 2.68e-13 | 0 | 0 |
| `beta <= 1e12` | 116 | 116 | 0 | 2.68e-13 | 0 | 0 |

**Identical at every threshold over six orders of magnitude**, so the guard is not tuned and is not doing hidden
work — it removes a disjoint, unambiguous population. Restricted to deep *and* drifting rows: **71 rows, max
`rho_full` 2.68e-13, max drift 923%**.

**So the write-up's numbers are right and its row selection was not stated.** Its "110 B3-holding rows, max
2.7e-13" is 116 under the principled rule, with the same maximum. The exclusions were defensible on the merits and
undocumented — which under this project's rules is the thing that has to change, not the number.

**Required fix:** add an adapter-norm divergence guard alongside the drift guard, and record the three outcomes
(real / vacuous / diverged) per claim rather than a pass count. Then every row admitted by the stated criteria is
one the headline actually contains.

## F10 — what this does to the verdict

The load-bearing check **PASSES**, on 71 deep drifting rows at 923% drift with a stated and untuned exclusion rule,
and the rank law matches on **116 of 116**. It does not pass as the write-up presents it, because the presented
population and the measured population differ by an unstated filter, and a reader recomputing from the rows gets
`0.71` and concludes the track is falsified. That gap is the audit's main deliverable.

Note the rank law that matches 116/116 is the **corrected** one, `max(0, min(r-N', n_l-N'))` (F2), not the
`r - N'` in Prop. A's statement — the harness computes the corrected form. F2 and F9 are therefore the same defect
seen from two sides: the statement is wrong, the code is right, and the agreement is real but is agreement with the
code.

## Still outstanding

`theory_checks` jobs **674726, 683234, 686846** are not yet read row by row (688036 is, in F4). T1, T3, T4, T6
remain unaudited. The verdict above covers T2 Prop. A and Cor. A.1 and T5.1 only.

---

# F11 — **T5.2 IS FALSE AS STATED**, and T5.3's planning number depends on it (register: derived + read-rows)

Status in the file: **PROVED**. It is not. Counterexample constructed by derivation, then confirmed numerically
(job **350996**, `experiments/multilayer_cert/t52_counterexample.py`): **0 of 12 seeds** match T5.2; the corrected
law matches **12 of 12**.

## Where the proof leaks

T5.2 claims `rank J_F = min(k_1, sum_l q_l)` a.s. The upper bound is right. The **equality** assumes the per-layer
row spaces `row(Ctil_l M_l)` spread independently inside `row(M_1)`. They cannot, and **T5.1 is the reason**:

    M_l = J_{psi_l} M_{l-1}  =>  row(M_1) ⊇ row(M_2) ⊇ ... ⊇ row(M_L).

`row(Ctil_l M_l) ⊆ row(M_l)`, and when `q_l = rank M_l` the containment is **equality** — the layer contributes
`row(M_l)` exactly. So two deep layers whose row spaces have collapsed to the same small subspace contribute *the
same subspace twice*. T5.2 counts them twice. Independence of `A_{l,0}` across layers does not help: the summands
are independently *oriented* only within a space they are already confined to.

## The corrected law

    rank J_F = min_j ( d_j + sum_{l<j} q_l ),    d_j := rank M_j,  j = 1..L+1,  d_{L+1} := 0.

Each term says: whatever the first `j-1` layers contributed, everything from layer `j` on is trapped inside
`row(M_j)`, of dimension `d_j`. T5.2 is the `j = 1` and `j = L+1` terms only — it omits every intermediate
constraint.

## The measurement

`d = [10, 3, 3]`, `q = [5, 3, 3]`, `k_1 = 10`, `r = 13`, `N = 8`, three adapted layers, twelve seeds:

| | T5.2 predicts | corrected predicts | measured |
|---|---|---|---|
| all 12 seeds | **10** | **8** | **8** |

The gap is not a tolerance artefact: it is an integer rank, identical across every seed.

## What this does to T5.3, which is a live planning number

T5.3's second regime — `r - N < k_1` implies `L >= k_1/(r-N)` adapted layers suffice generically — is derived from
T5.2 and **inherits the error**. It is true only when `rank M_l = k_1` for **every** adapted layer, i.e. when the
frozen path between adapted layers preserves the chart's rank. Where the network contracts rank between adapted
layers, the ceiling is `min_j(d_j + sum_{l<j} q_l)` and **adding layers past the point where `d_j` has collapsed
buys nothing at all**.

So the planning figure in circulation — *8 adapted layers for `k_1 = 128` at `r = 24, N = 8`* — is an **upper bound
on what depth can deliver, not an estimate**, and it is only attained if the frozen path is rank-preserving between
the adapted layers. That is a concrete, checkable precondition on the architecture: measure `rank M_l` at each
adapted layer before committing to a depth budget.

## Corrected status

| item | was | should be |
|---|---|---|
| T5.1 | PROVED | PROVED (confirmed independently) |
| T5.2 | PROVED | **FALSE as stated**; the corrected law is a candidate and is confirmed 12/12 on the counterexample family, but is not yet proved |
| T5.3 | PROVED | **conditional** on `rank M_l = k_1` at every adapted layer; states an upper bound otherwise |
| T5.4 | CONJECTURE | CONJECTURE (unchanged; and it now sits on top of a corrected T5.2) |

R2 (independence of `A_{l,0}` across layers) is **not** where this fails — the failure survives perfectly
independent seeds, because the constraint is geometric (nesting) rather than probabilistic. R2 remains a live
concern for tied or shared adapters, but it is not the leak.

---

# F12 — M3, the sweep built to test T5.2, is run entirely where T5.2 cannot be wrong (register: read-rows)

`results/multilayer_cert/survival_692603.jsonl`, 45 sane multi-layer stacks, **180 of 180 layers have `q_l`
saturated at `r - N`** (`r=16, N=3`, so `q_l = 13` at every layer of every stack). Saturation means
`rank M_l >= r - N`: the **per-layer budget** binds, never the **transmitted rank**. That is precisely the regime in
which the nesting cannot bite, so T5.2 and the corrected law **coincide identically** — both return `k_1 = 20`, and
both match the measured `rank_stacked = 20`.

**So this sweep provides no evidence for T5.2's additivity in the regime where additivity could fail.** It is
consistent with T5.2 and equally consistent with its refutation. A third instance of the family this audit keeps
finding: a check that passes without being able to fail.

To discriminate, a stack needs at least one layer with `rank M_l < r - N` — i.e. a frozen path that contracts below
the per-layer budget. None exists in this sweep.

# F13 — §19a already contains the discriminating measurement, on a real encoder ladder, and T5.2 fails it

`notes/assumption_relaxation_program.md` §19a, four adapted layers, `r = 256`, everything below frozen, no chart
(so `G = identity`, `M_j = J_{Phi_j^0}`, and their **transmitted rank is exactly my `d_j`** — the identification
holds).

**Depth 7 does not discriminate.** `d_1 = 138` caps both laws; both predict 138 and 138 is measured. Consistent
with everything.

**Depth 4 does.** The transmitted ranks are `d = [692, 220, 187, 138]` and `r - N = 256 - N ~ 248`, so the later
encoders sit **below** the per-layer budget — the discriminating regime F12 lacks.

| | prediction | measured |
|---|---|---|
| T5.2 `min(k_1, sum_l q_l)` | **692** | |
| corrected `min_j(d_j + sum_{l<j} q_l)`, binding term `d_2 + q_1 = 220 + q_1` | 220 + `q_1` | |
| | | **445** |

**T5.2 overshoots by 36% on real measured data at `r = 256`.** The corrected law's binding term is the `j = 2`
constraint and reproduces 445 exactly at `q_1 = 225`; I cannot confirm `q_1` because §19a records totals and not
per-layer `q_l`, so I state this as *consistent with* the corrected law and *refuting* T5.2, not as a two-sided
confirmation.

The write-up's own sentences describe the mechanism without naming it: *"the 3rd and 4th adapted layers add
literally nothing, their own encoders (96, 85) being nested inside the first's"* and *"the transmitted rank at the
STARTING depth is a tight upper bound, attained exactly when small enough to bind."* That is the `d_j` trapping
term, measured twelve days before it was derived.

# F14 — the depth route's ceiling is already measured, and it is unfavourable

The precondition F11 asks for — measure `rank M_l` at each adapted layer before committing a depth budget — is
**already answered** for the no-chart case by §19a's ladder:

    784 -> 692 -> 445/220 -> 187 -> 138 -> 96 -> 85

It contracts hard. So the depth route's ceiling is not merely bounded in principle; on this ladder the contraction
binds **well before eight layers at realistic depth**, and at depth 7 the third and fourth adapted layers
contribute *nothing measurable at all*. A plan costing `L >= k_1/(r-N)` layers needs **re-costing against the
measured ladder**, not re-testing.

Two scope clauses. This ladder is the **no-chart** case; with a chart, `M_j = J_{Phi_j^0} J_G` and the ranks can
only be smaller, so the contraction is at least this severe. And it is one architecture — the *mechanism* is
general, the *numbers* are not.

---

# F15 — M4/M5 verified at the rows: the numbers are real, the job id is wrong, and one check recorded a claim AND its negation as `passed` (register: read-rows)

Ledger rows **M4** and **M5** are registered `read-prose (GM)`. Reading them at source:

**The job attribution is wrong.** Both cite job **692603**. That job is `survival_692603.jsonl`, whose only
configuration is `r=16, N=3, r-N=13, k=20` across 45 stacks — it contains **no** `r-N = 9` cell and **no**
`[9, 18, 20, 20]`. The numbers are real but come from the **`theory_checks`** jobs (674726, 683234, 686846,
688036), where `T5.1_nesting_ceiling` records `ranks = [9,18,20,20], k1 = 20` and `T5.2_additivity_min_k1_sum_q`
records `observed = predicted = [9,18,20,20]`. **M4 and M5 should cite the theory_checks jobs.** Promoted from
read-prose to **read-rows**, numbers confirmed, provenance corrected.

**M4 does not discriminate, and that is correct behaviour.** `k=20`, four layers, `r-N=9`, a random FP64 MLP whose
frozen path is rank-preserving, so `d_j = k_1 = 20` at every layer. Then `min_j(d_j + sum_{l<j} q_l)` returns
9, 18, 20, 20 as well: **the corrected law reduces to T5.2 exactly when nothing contracts.** M4 is the
non-contracting side of the regime boundary; §19a (F13) is the contracting side. Neither cell was built to test the
distinction and together they bracket it.

## The defect: the same measurement recorded as confirming a claim and as refuting it, `passed: true` both times

| job | check name | `shared` | `independent` | `collapsed` | conclusion recorded | `passed` |
|---|---|---|---|---|---|---|
| **674726** | `T5_R2_shared_seed_collapses_additivity` | `[9,18,20,20]` | `[9,18,20,20]` | — | *"R2 is a **DEFENCE**: tie the adapter initialisations and the layers stop adding"* | **true** |
| 683234 / 686846 / 688036 | `T5_R2_shared_seed_observation` | `[9,18,20,20]` | `[9,18,20,20]` | `false` | *"tying `A_0` did **NOT** reduce the stacked rank ... R2 is **NOT** a defence"* | **true** |

**The data are byte-identical in both.** `shared == independent` means tying the seed changed nothing, which is
unambiguously the *later* reading. The first run recorded the opposite conclusion from the same numbers, under a
check name asserting the conclusion, and marked it **passed**.

So `passed: true` in that row meant *the code ran and its output was read as confirming the hypothesis* — it
carried no discriminating content whatever. This is the **fourth** instance in this audit of a check that cannot
fail: after the rank check that was a regression test on its own implementation (F2), the load-bearing check whose
exclusions were unstated (F9), and M3 run entirely where the two laws coincide (F12).

**Consequence for the record.** The repository still contains job 674726's row asserting *"R2 is a DEFENCE ...
layers stop adding"* with `passed: true`, and a future reader can cite it in good faith. It should be marked
superseded in place rather than left to be rediscovered. The correct statement, now supported three ways —
my counterexample (independent seeds, still fails), the geometric argument (the constraint is nesting, not
probability), and this measurement (shared == independent) — is that **R2 is not a defence and not the leak in
T5.2.**

**A naming rule this suggests:** a check whose *name* asserts its conclusion (`..._collapses_additivity`) will be
read as having confirmed it whenever it passes. Name checks for the quantity measured, not the expected
verdict — `T5_R2_shared_seed_observation`, which is what the later runs use.

---

# F16 — the theorem as stated WAS tested, and it FAILED twice; the code was corrected and the theorem was not (register: read-rows, inference named)

F2 reported that the harness implements `max(0, min(r-N', n_l-N'))` while Prop. A states `r - N'`, so the 116/116
agreement is agreement with the code. The four `theory_checks` jobs carry the history, and it is worse than a
mismatch.

| job | git | `T2_rank_C_equals_r_minus_Nprime` | recorded `expect_rank_C` | consistent with |
|---|---|---|---|---|
| 674726 | `ba9ca00` | **FAIL** | 21 at `N'=3`; 23 at `N'=1` | `r - N'` (the theorem **as stated**) |
| 683234 | `a2f71c6` | **FAIL** | 21 at `N'=3`; 9 at `N'=15` | `r - N'` (as stated) |
| 688036 | `de8b0ae` | **PASS** | **13** at `N'=3`; **5** at `N'=15` | `min(r-N', n_l-N')` (the **corrected** law) |

So the check testing the rank law **as the theorem states it failed twice**, and passed only once the expectation
was changed to the corrected formula. **The theorem was never updated, and the check still carries the name
`..._equals_r_minus_Nprime` while testing something else.**

That reframes F2. It is not that a theorem and its implementation quietly disagree — it is that **the theorem was
tested, it failed, and the resolution was to correct the code and leave the statement.** The disagreement the check
existed to detect was not merely engineered away before it ran; it was *observed*, twice, and then removed.

*Inference named:* `theory_checks.py` was untracked until it was swept into commit `691ac2e`, so I cannot diff the
source across those runs. The above is read from the recorded `expect_rank_C` values and the pass/fail verdicts,
which are data. The conclusion — that the expectation formula changed between `a2f71c6` and `de8b0ae` — is an
inference from those rows, not a diff.

# F17 — the fifth instance, and the load-bearing check's own pass history contains a false pass

Job **674726**, the load-bearing `T2_PropA_full_cert_exact_at_large_drift`: **`passed: true`**.

| layer | drift | B3 | `rho_base` |
|---|---|---|---|
| 0 | 0.000 | False | **nan** |
| 1 | 8.5e+73 | True | **nan** |
| 2 | 1.1e+151 | True | **nan** |
| 3 | **inf** | True | **nan** |

with `max_rho_base = 0.00e+00` and `exercised = None`. Every measured value is NaN, the network has diverged to
infinity, and the check reports a maximum residual of exactly zero and passes.

This is the failure LESSONS_LEARNED 2026-09-07 already names — a diverged run makes every downstream number NaN,
which silently passes a `max()`-based tolerance test — and the later runs fix it with an explicit non-finite trap
and the `exercised` filter. **So it is known and fixed.** What is worth recording is that **the pass history of the
check whose failure falsifies the track contains a pass that was false**, and job 683234 then correctly **FAILED**
it with `exercised = []` (no deep drifting B3 layer existed — the vacuity guard working as designed).

The honest summary of that check across four runs: **one false pass, one correct fail, one crash, one genuine
pass.** Only the last is evidence, and F9's guard is what makes it so.

## The family, complete

Five instances, five distinct mechanisms, all reporting success without the capacity to report failure:

1. **F2/F16** — a regression test on the implementation wearing a theorem's name; the theorem itself failed twice.
2. **F9** — the load-bearing check passing under an unstated exclusion; recomputing from the rows gives 0.71.
3. **F12** — M3 run entirely in the regime where the two competing laws coincide.
4. **F15** — a check recording a claim and its negation from byte-identical data, `passed` both times.
5. **F17** — a pass on an all-NaN diverged run, reporting `max = 0.00e+00`.

The general check that catches all five: **state the outcome that would have falsified the claim, and verify the
configuration is capable of producing it.**
